# Generated Leaders

`leaders/` is the local output directory for generated leader instances.

The repository keeps the folder itself, but ignores generated profiles by default so
private workplace material does not get committed by accident.
