# v0.1.2

## What changed

- rewrote the README into a denser, text-first landing page
- aligned the information architecture more closely with the reference skill repos
- strengthened the top-of-page hook, question stack, anchor navigation, and example section
- refreshed the sample bundle README to feel more like an immediately explorable demo
- kept the code and command surface intact while improving the public-facing presentation

## Why it matters

This iteration is about conversion:

- clearer first screen
- stronger meme energy
- easier to understand without images
- closer to the README rhythm that made the reference projects spread

## Validation

- `python -m unittest discover -s tests -p "test_*.py"`
- `python -m tools.cli translate --text "这个你先看一下，晚点我们再对齐。"`
- `python -m tools.cli create-leader --slug review-boss --name "复刻验证领导" --text "这个不复杂吧，你今天先出个方案，明天我们同步一下。"`
- `python -m tools.cli show-leader --slug review-boss`
- `python -m tools.cli delete-leader --slug review-boss --yes`
